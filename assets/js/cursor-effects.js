// Cursor Effects Initialization
// Initializes fairy dust cursor effect when page loads

window.addEventListener("load", (event) => {
    new cursoreffects.fairyDustCursor({
        colors: ["#fffafa"]
    });
});